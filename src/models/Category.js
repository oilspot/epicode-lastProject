export default class Category {
    constructor(id, name, slug, description) {
        this.id = id;
        this.name = name;
        this.slug = slug;
        this.description = description;
    }
}